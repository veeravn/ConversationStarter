apiVersion: v1
kind: Service
metadata:
  name: {{ include "conversation-starter.fullname" . }}
  labels:
    app: {{ include "conversation-starter.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "conversation-starter.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
